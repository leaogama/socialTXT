version: '3.8'

services:
  mcp-social-summary:
    build: .
    container_name: mcp-social-summary
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./cookies.txt:/app/cookies.txt:ro
    ports:
      - "8000:8000"
